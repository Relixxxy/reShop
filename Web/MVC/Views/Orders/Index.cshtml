@model IEnumerable<MVC.ViewModels.OrderVM>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="orders">
    <div class="container">
        <ul class="orders__list list">
            @foreach (var order in Model)
            {
                <partial name="_order" model="order" />
            }
        </ul>
    </div>
</section>

